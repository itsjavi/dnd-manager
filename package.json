{
  "name": "dnd-manager",
  "version": "1.0.0",
  "homepage": "https://itsjavi.com/dnd-manager",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/itsjavi/dnd-manager.git"
  },
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.mjs"
    }
  },
  "main": "./dist/index.mjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsdown src/index.ts --dts --sourcemap --format esm --clean",
    "format": "pnpx sort-package-json && pnpm exec prettier --cache --write .",
    "prepublishOnly": "pnpm typecheck && pnpm build && publint",
    "serve:e2e": "pnpm build && python3 -m http.server 4173 --directory .",
    "test": "vitest --run",
    "test:e2e": "pnpm build && vitest --project e2e",
    "test:unit": "vitest --project unit",
    "test:coverage": "pnpm build && vitest --run --coverage",
    "test:watch": "vitest --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/browser-playwright": "^4.0.18",
    "@vitest/coverage-v8": "^4.0.18",
    "jsdom": "^28.1.0",
    "prettier": "^3.8.1",
    "publint": "^0.3.17",
    "sort-package-json": "^3.6.1",
    "tsdown": "^0.20.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
